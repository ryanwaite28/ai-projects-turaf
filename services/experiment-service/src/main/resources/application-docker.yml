spring:
  application:
    name: experiment-service
  
  datasource:
    url: jdbc:postgresql://postgres:5432/${DB_NAME:turaf}?currentSchema=experiment_schema
    username: ${DB_USERNAME:experiment_user}
    password: ${EXPERIMENT_USER_PASSWORD}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: experiment_schema
  
  flyway:
    enabled: false

jwt:
  secret: ${JWT_SECRET}

turaf:
  security:
    service-mode: true

server:
  port: 8083

aws:
  region: ${AWS_REGION:us-east-1}
  endpoint: ${AWS_ENDPOINT:}
  eventbridge:
    bus-name: ${EVENTBRIDGE_BUS:turaf-event-bus}

logging:
  level:
    com.turaf: DEBUG
    org.springframework: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
